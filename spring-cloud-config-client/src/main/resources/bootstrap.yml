spring:
  cloud:
    bus:
      trace:
        enabled: true
    config:
      name: application
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: spring-cloud-config-server


eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8000/eureka/
